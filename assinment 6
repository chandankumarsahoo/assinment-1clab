#include <stdio.h>

int main ()
{
  float c, f;
  printf ("Enter tempareture in Fahrenheit  :-   ");
  scanf ("%f", &f);
  c = ((f - 32) / 9) * 5;

  printf ("Tempareture in Celsius =%f", c);

  return 0;
}
